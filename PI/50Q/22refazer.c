#include <stdio.h>
#include <string.h>

contida(char a[], char b[])
{
    int i, j, lena, lenb;
    lena = (strlen(a));
    lenb = (strlen(b));

    for (i = 0; i < lena; i++)
    {

        for (j = 0; j < lenb; j++)
        {
            {
                if (a[i] = b[j])
                    ;
            }
        }
    }
}